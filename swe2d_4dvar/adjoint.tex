\documentclass[12pt,a4paper]{article}
\usepackage[fontset=none]{ctex}
\setCJKmainfont{SimSun}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,bm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{fancyhdr}

\hypersetup{colorlinks,linkcolor=blue,citecolor=blue,urlcolor=blue}
\setlength{\headheight}{15pt}
\setlength{\emergencystretch}{3em}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{二维浅水方程 4D-Var 离散伴随}
\fancyhead[R]{\thepage}

\title{二维浅水方程 4D-Var 离散伴随说明\\
\large 与 \texttt{swe2d\_4dvar.c} 保持一致}
\author{杨鹏亮}
\date{\today}

\begin{document}

\maketitle

\begin{abstract}
本文只记录当前程序 \texttt{swe2d\_4dvar.c} 实际实现的离散伴随。
前向模式是二维浅水方程的有限体积离散，界面通量为 Rusanov 通量，
时间推进为两次 Euler 子步组成的 RK2。4D-Var 控制变量是初始全状态，
观测只抽取若干网格点上的水深。梯度由完整前向轨道、逐步反向的
离散伴随和背景项共同组成。本文不再推导连续伴随 PDE，因为代码没有
使用连续伴随后再离散的路线。
\end{abstract}

\tableofcontents

\section{状态、网格和代价函数}

单元状态为守恒变量
\[
\mathbf{q}_{i,j} =
\begin{pmatrix}
h_{i,j}\\
(hu)_{i,j}\\
(hv)_{i,j}
\end{pmatrix}.
\]
代码中一个完整场用三个数组 \texttt{h}, \texttt{hu}, \texttt{hv} 保存，
单元索引为
\[
k=i+N_x j.
\]

当前 4D-Var 只优化初始场 $\mathbf{Q}^0\in\mathbb{R}^{3N}$。背景场记为
$\mathbf{Q}_b$，观测位于每个同化窗口末端。代价函数为
\begin{equation}
J(\mathbf{Q}^0)
=
\frac12(\mathbf{Q}^0-\mathbf{Q}_b)^T B^{-1}(\mathbf{Q}^0-\mathbf{Q}_b)
+\frac12\sum_{c=1}^{N_c}
\left(H\mathbf{Q}^{n_c}-\mathbf{y}_c\right)^T
R^{-1}
\left(H\mathbf{Q}^{n_c}-\mathbf{y}_c\right).
\label{eq:cost}
\end{equation}
这里 $N=N_xN_y$，$n_c=c\,\mathtt{VAR\_STEPS\_WINDOW}$。
$H$ 是观测算子，从全状态中抽取观测单元的水深 $h$ 分量：
\[
H\mathbf{Q}^{n_c}
=
\left(h_{o_1}^{n_c},\ldots,h_{o_{N_{\rm obs}}}^{n_c}\right)^T,
\]
其中 $o_p$ 是第 $p$ 个观测单元索引。

矩阵 $B$ 是背景误差协方差矩阵；当前代码采用逐格点、逐变量独立的对角形式
\[
B=\operatorname{diag}\left(
\sigma_h^2 I_N,\,
\sigma_m^2 I_N,\,
\sigma_m^2 I_N
\right).
\]
矩阵 $R$ 是观测误差协方差矩阵；当前代码假设所有水深观测误差相互独立且方差相同，
\[
R=\sigma_o^2 I_{N_{\rm obs}}.
\]
因此式 \eqref{eq:cost} 展开后正是代码中
\texttt{evaluate\_cost\_gradient()} 计算的逐分量加权平方和。

\section{前向离散模式}

\subsection{物理通量}

令 $m=hu$，$n=hv$。物理通量为
\begin{equation}
\mathbf{f}(\mathbf{q}) =
\begin{pmatrix}
m\\
m^2/h+\frac12 g h^2\\
mn/h
\end{pmatrix},
\qquad
\mathbf{g}(\mathbf{q}) =
\begin{pmatrix}
n\\
mn/h\\
n^2/h+\frac12 g h^2
\end{pmatrix}.
\label{eq:physical-flux}
\end{equation}
代码在分母中使用 \texttt{safe\_h()}，即 $h$ 小于 \texttt{HMIN} 时用
\texttt{HMIN} 代替。

\subsection{Rusanov 通量}

$x$ 方向界面通量为
\begin{equation}
\widehat{\mathbf{f}}(\mathbf{q}_L,\mathbf{q}_R)
=
\frac12\left[\mathbf{f}(\mathbf{q}_L)+\mathbf{f}(\mathbf{q}_R)\right]
-\frac12 s(\mathbf{q}_R-\mathbf{q}_L),
\label{eq:rusanov-x}
\end{equation}
其中
\[
s=\max(a_L,a_R),
\qquad
a(\mathbf{q})=|u|+\sqrt{g h},
\qquad
u=m/h.
\]
$y$ 方向完全类似，只是速度用 $v=n/h$：
\[
a_y(\mathbf{q})=|v|+\sqrt{g h}.
\]
这些式子对应 \texttt{rusanov\_x()} 和 \texttt{rusanov\_y()}。

\subsection{Euler 子步}

\texttt{euler\_update()} 实现
\begin{equation}
\mathbf{q}_{i,j}^{+}
=
\mathbf{q}_{i,j}
-\frac{\Delta t}{\Delta x}
\left(
\widehat{\mathbf{f}}_{i+1/2,j}
-\widehat{\mathbf{f}}_{i-1/2,j}
\right)
-\frac{\Delta t}{\Delta y}
\left(
\widehat{\mathbf{g}}_{i,j+1/2}
-\widehat{\mathbf{g}}_{i,j-1/2}
\right).
\label{eq:euler}
\end{equation}
越界邻居由 \texttt{get\_state\_bc()} 钳制到最近的边界单元，因此边界是
离散零梯度延拓。

\subsection{RK2 步}

\texttt{step\_rk2\_store()} 使用
\begin{align}
\mathbf{Q}^{(1)} &= E(\mathbf{Q}^n),\\
\mathbf{Q}^{(2)} &= E(\mathbf{Q}^{(1)}),\\
\mathbf{Q}^{n+1} &= \frac12\mathbf{Q}^{n}+\frac12\mathbf{Q}^{(2)}.
\label{eq:rk2}
\end{align}
其中 $E$ 是式 \eqref{eq:euler} 的 Euler 子步。代码保存
\texttt{traj[n]} 和 \texttt{stage1[n]}，分别对应 $\mathbf{Q}^n$ 和
$\mathbf{Q}^{(1)}$，供离散伴随反向使用。

\section{界面通量的 Jacobian}

离散伴随需要 Rusanov 通量对两侧状态的导数。物理通量 Jacobian 为
\begin{equation}
A(\mathbf{q})=\frac{\partial\mathbf{f}}{\partial\mathbf{q}}
=
\begin{pmatrix}
0 & 1 & 0\\
gh-m^2/h^2 & 2m/h & 0\\
-mn/h^2 & n/h & m/h
\end{pmatrix},
\label{eq:Ax}
\end{equation}
\begin{equation}
B(\mathbf{q})=\frac{\partial\mathbf{g}}{\partial\mathbf{q}}
=
\begin{pmatrix}
0 & 0 & 1\\
-mn/h^2 & n/h & m/h\\
gh-n^2/h^2 & 0 & 2n/h
\end{pmatrix}.
\label{eq:Ay}
\end{equation}
代码中的 \texttt{flux\_jacobian\_x()} 和 \texttt{flux\_jacobian\_y()}
还包含 \texttt{safe\_h()} 分支：当原始 $h\le\mathtt{HMIN}$ 时，关于
$h$ 的导数被置零，以匹配前向分支。

对 $x$ 方向 Rusanov 通量，定义
\[
\Delta\mathbf{q}=\mathbf{q}_R-\mathbf{q}_L.
\]
若 $a_L\ge a_R$，则 $s=a_L$，$\nabla_{\mathbf{q}_L}s=\nabla a_L$，
$\nabla_{\mathbf{q}_R}s=0$；否则相反。于是
\begin{align}
\widehat{F}_L
&=
\frac12 A(\mathbf{q}_L)+\frac12sI
-\frac12\Delta\mathbf{q}\,(\nabla_{\mathbf{q}_L}s)^{T},
\label{eq:FL}\\
\widehat{F}_R
&=
\frac12 A(\mathbf{q}_R)-\frac12sI
-\frac12\Delta\mathbf{q}\,(\nabla_{\mathbf{q}_R}s)^{T}.
\label{eq:FR}
\end{align}
这正是 \texttt{rusanov\_x\_jacobians()} 的计算。$y$ 方向的
\texttt{rusanov\_y\_jacobians()} 用同样公式，只把左右状态换成
bottom/top 状态，并用 $B(\mathbf{q})$ 和 $a_y$。

速度导数在代码中为
\begin{equation}
\nabla a_x =
\begin{pmatrix}
-\operatorname{sgn}(u)m/h^2+\frac12 g/\sqrt{gh}\\
\operatorname{sgn}(u)/h\\
0
\end{pmatrix},
\qquad
\nabla a_y =
\begin{pmatrix}
-\operatorname{sgn}(v)n/h^2+\frac12 g/\sqrt{gh}\\
0\\
\operatorname{sgn}(v)/h
\end{pmatrix}.
\label{eq:wavespeed-grad}
\end{equation}
在 $u=0$ 或 $v=0$ 时，\texttt{signum()} 返回零。

\section{Euler 子步的离散伴随}

设某个输出单元 $(i,j)$ 的伴随为
\[
\bm{\lambda}_{i,j}^{+}
=
\begin{pmatrix}
\lambda_h^+\\
\lambda_{hu}^+\\
\lambda_{hv}^+
\end{pmatrix}.
\]
\texttt{euler\_adjoint()} 对每个输出单元做反向累加。首先，前向 Euler
子步中有恒等项 $\mathbf{q}_{i,j}^{+}\leftarrow\mathbf{q}_{i,j}$，因此
\[
\bm{\lambda}_{i,j}\mathrel{+}= \bm{\lambda}_{i,j}^{+}.
\]

然后四个界面按前向式 \eqref{eq:euler} 中的符号回传：
\begin{align}
\text{右界面:}\quad
\bm{\lambda}_{i,j}   &\mathrel{+}= -\frac{\Delta t}{\Delta x}\widehat{F}_L^T\bm{\lambda}_{i,j}^{+},
&
\bm{\lambda}_{i+1,j} &\mathrel{+}= -\frac{\Delta t}{\Delta x}\widehat{F}_R^T\bm{\lambda}_{i,j}^{+},
\label{eq:adj-right}\\
\text{左界面:}\quad
\bm{\lambda}_{i-1,j} &\mathrel{+}=  \frac{\Delta t}{\Delta x}\widehat{F}_L^T\bm{\lambda}_{i,j}^{+},
&
\bm{\lambda}_{i,j}   &\mathrel{+}=  \frac{\Delta t}{\Delta x}\widehat{F}_R^T\bm{\lambda}_{i,j}^{+},
\label{eq:adj-left}\\
\text{上界面:}\quad
\bm{\lambda}_{i,j}   &\mathrel{+}= -\frac{\Delta t}{\Delta y}\widehat{G}_B^T\bm{\lambda}_{i,j}^{+},
&
\bm{\lambda}_{i,j+1} &\mathrel{+}= -\frac{\Delta t}{\Delta y}\widehat{G}_T^T\bm{\lambda}_{i,j}^{+},
\label{eq:adj-top}\\
\text{下界面:}\quad
\bm{\lambda}_{i,j-1} &\mathrel{+}=  \frac{\Delta t}{\Delta y}\widehat{G}_B^T\bm{\lambda}_{i,j}^{+},
&
\bm{\lambda}_{i,j}   &\mathrel{+}=  \frac{\Delta t}{\Delta y}\widehat{G}_T^T\bm{\lambda}_{i,j}^{+}.
\label{eq:adj-bottom}
\end{align}
矩阵转置乘向量由 \texttt{add\_matT\_vec\_to\_cell()} 完成。

边界处使用和前向相同的钳制索引。例如 $i=0$ 时，左邻居仍然是
$i=0$；因此边界单元会收到来自自身和钳制 ghost 路径的累加贡献。
这不是连续边界条件的近似，而是前向离散索引映射的精确转置。

\section{RK2 步的离散伴随}

由式 \eqref{eq:rk2} 可得反向顺序：
\begin{align}
\bm{\Lambda}^{(2)} &= \frac12\bm{\Lambda}^{n+1},\\
\bm{\Lambda}^{n}   &= \frac12\bm{\Lambda}^{n+1},\\
\bm{\Lambda}^{(1)} &= E'(\mathbf{Q}^{(1)})^T\bm{\Lambda}^{(2)},\\
\bm{\Lambda}^{n}   &\mathrel{+}= E'(\mathbf{Q}^{n})^T\bm{\Lambda}^{(1)}.
\label{eq:rk2-adj}
\end{align}
这对应 \texttt{rk2\_adjoint\_step()}：
\begin{itemize}
\item \texttt{lam\_stage2} 接收 $\frac12\bm{\Lambda}^{n+1}$；
\item \texttt{lam\_n} 先接收 $\frac12\bm{\Lambda}^{n+1}$；
\item 在 \texttt{Q1} 上调用一次 \texttt{euler\_adjoint()}；
\item 在 \texttt{Qn} 上再调用一次 \texttt{euler\_adjoint()}；
\item 最后把第二次反传结果累加到 \texttt{lam\_n}。
\end{itemize}

\section{观测项和全窗口梯度}

观测算子只抽取水深：
\[
H\mathbf{Q}^{n_c}
=
\left(
h_{o_1}^{n_c},\ldots,h_{o_{N_{\rm obs}}}^{n_c}
\right)^T.
\]
因此在观测时刻只向 $h$ 分量注入
\begin{equation}
\lambda_h(o_p)\mathrel{+}=
\frac{h_{o_p}^{n_c}-y_{c,p}}{\sigma_o^2}.
\label{eq:obs-adj}
\end{equation}
这对应 \texttt{obs\_gradient\_add()}。

\texttt{evaluate\_cost\_gradient()} 的顺序是：
\begin{enumerate}
\item 从当前初值 \texttt{x0} 前向积分，保存 \texttt{traj} 和 \texttt{stage1}；
\item 计算背景项，并把背景项梯度写入 \texttt{grad}；
\item 遍历所有观测时刻，累计观测代价；
\item 从末时刻反向到初始时刻；若当前时间层是观测时刻，先执行式
      \eqref{eq:obs-adj}，再反传一个 RK2 步；
\item 把回到 $t=0$ 的伴随 \texttt{lam\_curr} 加到背景项梯度上。
\end{enumerate}
最终梯度为
\begin{equation}
\nabla_{\mathbf{Q}^0}J
=
B^{-1}(\mathbf{Q}^0-\mathbf{Q}_b)+\bm{\Lambda}^{0},
\label{eq:grad}
\end{equation}
其中 $B$ 是对角矩阵，每个单元对应
\[
\operatorname{diag}(\sigma_h^2,\sigma_m^2,\sigma_m^2).
\]

\section{优化器}

代码使用有限记忆 BFGS（L-BFGS）优化初始场。第 $k$ 次迭代中，记
$g_k=\nabla J(\mathbf{Q}_k^0)$。每次线搜索接受新状态后保存曲率对
\[
s_k=\mathbf{Q}_{k+1}^0-\mathbf{Q}_k^0,\qquad
y_k=g_{k+1}-g_k,
\]
并在满足 $s_k^T y_k>0$ 时写入长度为 \texttt{LBFGS\_MEMORY} 的循环历史。
下一次迭代通过 L-BFGS two-loop recursion 近似
\begin{equation}
\mathbf{p}
=
 -H_k g_k,
\label{eq:direction}
\end{equation}
其中 $H_k$ 是由最近曲率对隐式定义的逆 Hessian 近似。初始缩放使用
\[
H_k^{(0)}
=
\gamma_k
\operatorname{diag}(\sigma_h^2 I_N,\sigma_m^2 I_N,\sigma_m^2 I_N),
\qquad
\gamma_k=\frac{s_{k-1}^T y_{k-1}}{y_{k-1}^T y_{k-1}},
\]
没有可用曲率对时退化为预条件负梯度方向。若 L-BFGS 方向不是下降方向，
代码会丢弃历史并重启到该预条件负梯度方向。

\texttt{line\_search\_two\_way()} 接受同时满足以下两个条件的步长：
\begin{align}
J(\mathbf{Q}^0+\alpha\mathbf{p})
&\le
J(\mathbf{Q}^0)+c_1\alpha\nabla J^T\mathbf{p},
\label{eq:armijo}\\
\nabla J(\mathbf{Q}^0+\alpha\mathbf{p})^T\mathbf{p}
&\ge
c_2\nabla J^T\mathbf{p}.
\label{eq:curvature}
\end{align}
若代价过大则缩小步长；若下降满足但曲率条件不满足，则尝试放大或二分。

\section{与代码一致的参数}

\begin{table}[htbp]
\centering
\caption{\texttt{swe2d\_4dvar.c} 中的默认宏参数}
\begin{tabular}{lcc}
\toprule
含义 & 宏 & 默认值\\
\midrule
网格规模 & \texttt{NX}, \texttt{NY} & $60,60$\\
区域大小 & \texttt{LX}, \texttt{LY} & $1,1$\\
观测点数 & \texttt{NOBS} & $16$\\
同化周期数 & \texttt{NCYCLES} & $30$\\
单周期窗口长度 & \texttt{ASSIM\_WINDOW} & $0.04$\\
每周期 RK2 步数 & \texttt{VAR\_STEPS\_WINDOW} & $20$\\
固定时间步长 & $\Delta t$ & $0.002$\\
观测误差标准差 & \texttt{OBS\_STD} & $0.01$\\
背景水深标准差 & \texttt{SIGMA\_H0} & $0.02$\\
背景动量标准差 & \texttt{SIGMA\_M0} & $0.005$\\
最大优化迭代数 & \texttt{VAR\_MAX\_ITER} & $12$\\
\bottomrule
\end{tabular}
\end{table}

\section{非光滑点}

代码中有三个非光滑或分段操作：
\begin{itemize}
\item \texttt{safe\_h()}：前向分母使用水深下限；Jacobian 中对低于下限的
      $h$ 分支把 $h$ 导数置零；
\item \texttt{max(aL,aR)}：Rusanov 速度导数只给达到最大值的一侧；相等时
      代码选择左侧或 bottom 侧；
\item \texttt{apply\_physical\_floor()}：若水深过小或出现非有限数，会重置整
      个单元。主推导没有给这个裁剪写伴随，实际实验应尽量避免触发它。
\end{itemize}
因此本文描述的是代码在正常光滑分支上的离散伴随。

\section{验证}

程序启动后会执行两类梯度检验。

\subsection{随机方向 Taylor 检验}

取单位方向 $\mathbf{d}$，比较
\begin{equation}
J(\mathbf{Q}^0+\varepsilon\mathbf{d})
-J(\mathbf{Q}^0)
-\varepsilon\nabla J^T\mathbf{d}.
\label{eq:taylor}
\end{equation}
若离散伴随梯度正确，该余项应为 $O(\varepsilon^2)$，直到舍入误差开始主导。
结果写入 \texttt{var\_output/fd\_adjoint\_check.csv}。

\subsection{抽样分量中心差分检验}

代码还从 $3N_xN_y$ 个控制分量中抽样，做中心差分
\begin{equation}
\frac{
J(\mathbf{Q}^0+\varepsilon\mathbf{e}_p)
-J(\mathbf{Q}^0-\varepsilon\mathbf{e}_p)
}{2\varepsilon}
\label{eq:component-fd}
\end{equation}
并与伴随梯度第 $p$ 个分量比较。结果写入
\texttt{var\_output/fd\_adjoint\_components.csv}。

\section{数值结果和图}

本节记录当前 \texttt{var\_output/} 中已有结果。
这些结果由 C 程序和绘图脚本生成。

\subsection{优化收敛证据}

与参考报告 \texttt{4dvar/main.pdf} 类似，先检查优化器是否在有限迭代内有效降低目标函数。
当前程序使用物理控制变量空间中的 L-BFGS，而不是参考报告中的标准化控制变量
PR+ 共轭梯度；因此这里展示的是本代码实际控制空间中的收敛行为。

优化历史见表 \ref{tab:opt-result}、图 \ref{fig:normalized-convergence} 和图
\ref{fig:var-metrics}。总代价从 $2.5877\times10^{2}$ 降到
$2.5231\times10^{2}$，下降 $6.4601$。梯度范数从
$1.6961\times10^{3}$ 降到 $2.4445\times10^{2}$。

\begin{table}[htbp]
\centering
\caption{4D-Var 优化摘要，来自 \texttt{cost\_history.csv}}
\label{tab:opt-result}
\begin{tabular}{lcc}
\toprule
指标 & 初始值 & 最终值\\
\midrule
总代价 $J$ & $2.587655\times10^{2}$ & $2.523054\times10^{2}$\\
梯度范数 $\|\nabla J\|$ & $1.696084\times10^{3}$ & $2.444523\times10^{2}$\\
接受步长 $\alpha$ & $1.5625\times10^{-2}$ & $1.2500\times10^{-1}$\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\textwidth]{var_output/var_4d_normalized_convergence.png}
\caption{归一化目标函数、归一化梯度范数和接受步长。该图对应参考报告中的优化收敛证据，但使用当前代码的 L-BFGS 优化器。}
\label{fig:normalized-convergence}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.96\textwidth]{var_output/var_4d_metrics.png}
\caption{背景轨道和分析轨道水深 RMSE、总代价、梯度范数和步长。}
\label{fig:var-metrics}
\end{figure}

\subsection{同化能力证据：background-to-analysis RMSE 降低}

参考报告用成对 RMSE 图判断 analysis 是否比 background 更接近 truth。
图 \ref{fig:paired-rmse} 对当前实验做同样检查。需要注意：当前代码只同化稀疏水深观测，
但控制变量仍是全状态 $h,hu,hv$，因此动量变量的改进来自浅水动力耦合和全窗口约束。

若只统计第 $1$ 到第 $30$ 个输出周期，平均相对 RMSE 降低率为
\[
\begin{aligned}
\rho_h  &= 69.93\%,\\
\rho_{hu} &= 12.65\%,\\
\rho_{hv} &= 23.71\%,
\end{aligned}
\]
其中 $\rho=1-\mathrm{RMSE}_{\mathrm{analysis}}/\mathrm{RMSE}_{\mathrm{background}}$。
末时刻 $t=1.2$ 时，三变量 RMSE 如表 \ref{tab:state-rmse} 所示。

\begin{table}[htbp]
\centering
\caption{末时刻全状态 RMSE，由绘图脚本重新读取场文件计算}
\label{tab:state-rmse}
\begin{tabular}{lccc}
\toprule
变量 & background RMSE & analysis RMSE & 相对降低率\\
\midrule
$h$  & $1.3781\times10^{-3}$ & $1.1279\times10^{-4}$ & $91.82\%$\\
$hu$ & $9.5377\times10^{-3}$ & $8.0935\times10^{-3}$ & $15.14\%$\\
$hv$ & $2.1811\times10^{-2}$ & $1.5722\times10^{-2}$ & $27.92\%$\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.96\textwidth]{var_output/var_4d_paired_rmse.png}
\caption{各输出周期上的 background-to-analysis RMSE 成对变化。前三个面板分别对应 $h,hu,hv$，最后一个面板为归一化全状态 RMSE。}
\label{fig:paired-rmse}
\end{figure}

\subsection{空间误差结构证据}

标量 RMSE 不能说明误差是否在空间结构上被削弱。图 \ref{fig:full-state-snapshot}
给出末时刻的全状态空间误差结构：真值场、背景误差、分析误差，以及
$|\mathrm{background\ error}|-|\mathrm{analysis\ error}|$。最后一列为正值时表示
analysis 在该位置降低了绝对误差。图 \ref{fig:var-snapshots} 则沿时间窗选取早期、
中期和末期三个水深快照，展示误差随窗口推进的变化。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.98\textwidth]{var_output/var_4d_full_state_snapshot.png}
\caption{末时刻 $h,hu,hv$ 的空间误差修正结构。水深观测点用空心圆标出。}
\label{fig:full-state-snapshot}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.96\textwidth]{var_output/var_4d_snapshots.png}
\caption{代表性时刻的水深真值场、背景误差、分析误差和改进场。}
\label{fig:var-snapshots}
\end{figure}

\subsection{预报式验证：整个窗口内的轨道 RMSE}

参考报告进一步检查 analysis 是否能作为更好的短期预报初值。当前程序不是循环逐窗
4D-Var，而是单个长窗口强约束 4D-Var；因此这里采用预报式验证：从同一个初始时刻出发，
比较 background 初值轨道与 analysis 初值轨道在整个窗口内相对 truth 的 RMSE。
图 \ref{fig:forecast-style} 显示，analysis 轨道在 $h$ 上有最明显改进，在 $hu,hv$ 上也有
正的相对 RMSE 降低，说明水深观测通过离散伴随和动力约束影响了动量控制分量。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.96\textwidth]{var_output/var_4d_forecast_style_rmse.png}
\caption{预报式轨道验证：background 初值轨道与 analysis 初值轨道在整个窗口内的 $h,hu,hv$ RMSE。虚线为相对 RMSE 降低率。}
\label{fig:forecast-style}
\end{figure}

\subsection{逐周期观测失配}

线搜索保证的是全窗口总代价下降，而不是每个周期的观测失配单调下降。
逐周期失配图见图 \ref{fig:cycle-misfit}。这个图用于检查不同观测时刻之间
如何竞争同一个初始条件控制变量。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\textwidth]{var_output/var_4d_cycle_misfit.png}
\caption{逐周期观测 RMS 失配随优化迭代的变化。}
\label{fig:cycle-misfit}
\end{figure}

\subsection{梯度检验结果}

随机方向 Taylor 检验的结果见表 \ref{tab:taylor-result} 和图
\ref{fig:fd-adjoint-check}。当 $\varepsilon$ 从 $10^{-1}$ 减小到
$10^{-5}$ 时，Taylor 余项从 $1.3782\times10^{2}$ 降到
$1.3782\times10^{-6}$，符合二阶余项的预期。最小扰动处
$\Phi=(J(x+\varepsilon d)-J(x))/(\varepsilon\nabla J^T d)$ 为
$9.9332\times10^{-1}$。

\begin{table}[htbp]
\centering
\caption{随机方向 Taylor 检验，来自 \texttt{fd\_adjoint\_check.csv}}
\label{tab:taylor-result}
\begin{tabular}{cccc}
\toprule
$\varepsilon$ & 有限差分方向导数 & 伴随方向导数 & Taylor 余项\\
\midrule
$10^{-1}$ & $1.3576\times10^{3}$ & $-2.0623\times10^{1}$ & $1.3782\times10^{2}$\\
$10^{-2}$ & $1.1720\times10^{2}$ & $-2.0623\times10^{1}$ & $1.3782\times10^{0}$\\
$10^{-3}$ & $-6.8407\times10^{0}$ & $-2.0623\times10^{1}$ & $1.3782\times10^{-2}$\\
$10^{-4}$ & $-1.9245\times10^{1}$ & $-2.0623\times10^{1}$ & $1.3782\times10^{-4}$\\
$10^{-5}$ & $-2.0485\times10^{1}$ & $-2.0623\times10^{1}$ & $1.3782\times10^{-6}$\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.86\textwidth]{var_output/fd_adjoint_check.png}
\caption{随机方向有限差分与伴随方向导数检验。}
\label{fig:fd-adjoint-check}
\end{figure}

抽样分量中心差分检验见图 \ref{fig:fd-adjoint-components}。当前抽样
$24$ 个控制分量，伴随梯度与中心差分梯度的相对 $L^2$ 差异为
$1.6403\times10^{-7}$，相关系数为 $1.000000000000$。最大绝对差异为
$1.0334\times10^{-4}$，出现在第 $469$ 个控制分量，即一个 $h$ 分量。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.90\textwidth]{var_output/fd_adjoint_components.png}
\caption{抽样控制分量上的中心有限差分与离散伴随梯度对比。}
\label{fig:fd-adjoint-components}
\end{figure}

\section{输出文件}

\texttt{swe2d\_4dvar.c} 直接写出的主要文件包括：
\begin{itemize}
\item \texttt{cost\_history.csv}：每次优化迭代的总代价、梯度范数和步长；
\item \texttt{iter\_cycle\_misfit.csv}：每次迭代、每个周期的观测代价和 RMS；
\item \texttt{trajectory\_metrics.csv}：背景轨道和分析轨道相对真值的水深 RMSE；
\item \texttt{obs\_config.csv}：观测点单元索引；
\item \texttt{fd\_adjoint\_check.csv}：随机方向 Taylor 检验；
\item \texttt{fd\_adjoint\_components.csv}：抽样分量有限差分检验；
\item \texttt{numeric\_summary.txt}：绘图脚本重新计算的全状态 RMSE 和相对降低率摘要；
\item \texttt{truth\_cycle*.csv}, \texttt{background\_cycle*.csv},
      \texttt{analysis\_cycle*.csv}：各输出时刻的场。
\end{itemize}
绘图脚本 \texttt{plot\_4dvar\_result.py} 会基于这些 CSV 生成收敛图、成对
RMSE 图、空间误差图、预报式 RMSE 图、逐周期失配图和梯度检验图。

\section{结论}

当前程序实现的是离散伴随，不是连续伴随 PDE 的再离散。保持一致的关键是：
\begin{enumerate}
\item 使用和前向完全相同的 Rusanov 分支、边界钳制和固定时间步长；
\item 对 Euler 子步逐界面累加 Jacobian 转置贡献；
\item 对 RK2 严格按组合映射反向传播；
\item 在观测时刻只向被观测水深分量注入 $R^{-1}(H\mathbf{Q}-\mathbf{y})$；
\item 最后把初始伴随加到背景项梯度上。
\end{enumerate}
这些步骤与 \texttt{swe2d\_4dvar.c} 中的函数调用顺序一一对应。

\end{document}
